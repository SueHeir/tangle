[package]
name = "tangle_contact"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Device-resident broad phase and capsule contact kernels for TANGLE"

[features]
default = ["wgpu", "cpu"]
wgpu = ["cubecl/wgpu"]
cpu = ["cubecl/cpu"]
cuda = ["cubecl/cuda"]
hip = ["cubecl/hip"]

[dependencies]
cubecl.workspace = true
